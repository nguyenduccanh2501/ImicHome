<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hn.bk.nguyenduccanh.imichome">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ThongTinCaNhanActivity" />
        <activity android:name=".HoiDapActivity" />
        <activity android:name=".DangKiHocActivity" />
        <activity android:name=".GiangVienActivity" />
        <activity android:name=".DanhSachKhoaHocActivity"></activity>
    </application>

</manifest>